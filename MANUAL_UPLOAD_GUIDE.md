# 📤 رفع المشروع بدون Git (يدوياً)

## الطريقة 1: رفع مباشر على GitHub

### الخطوة 1: إنشاء Repository
1. اذهب إلى: https://github.com/new
2. اسم Repository: `portail-sila`
3. اجعله **Private**
4. اضغط **Create repository**

### الخطوة 2: رفع الملفات
1. في صفحة Repository، اضغط **uploading an existing file**
2. اسحب وأفلت جميع الملفات من `d:\portail`
3. **لا ترفع:**
   - مجلد `node_modules`
   - مجلد `dist`
   - ملف `.env`
   - ملف `.env.local`
4. اضغط **Commit changes**

⚠️ **تحذير:** هذه الطريقة صعبة للتحديثات المستقبلية!

---

## الطريقة 2: رفع على VPS مباشرة (بدون GitHub)

### باستخدام FileZilla (SFTP)

#### الخطوة 1: تحميل FileZilla
- اذهب إلى: https://filezilla-project.org/download.php?type=client
- حمل وثبت FileZilla Client

#### الخطوة 2: الاتصال بـ VPS
1. افتح FileZilla
2. في الأعلى:
   - **Host:** `sftp://YOUR-VPS-IP`
   - **Username:** `root`
   - **Password:** كلمة مرور VPS
   - **Port:** `22`
3. اضغط **Quickconnect**

#### الخطوة 3: رفع الملفات
1. في الجانب الأيسر: انتقل إلى `d:\portail`
2. في الجانب الأيمن: انتقل إلى `/var/www/`
3. أنشئ مجلد `portail-sila`
4. اسحب وأفلت الملفات من اليسار إلى اليمين

**لا ترفع:**
- `node_modules/` (سيتم تثبيتها على VPS)
- `dist/` (سيتم بناؤها على VPS)
- `.env` و `.env.local` (ستنشئها على VPS)

#### الخطوة 4: إعداد الملفات على VPS
```bash
# اتصل بـ VPS
ssh root@YOUR-VPS-IP

# انتقل إلى المجلد
cd /var/www/portail-sila

# أنشئ ملف .env للـ Backend
cd backend
nano .env
# انسخ محتوى من backend/.env.example وعدل القيم

# أنشئ ملف .env.local للـ Frontend
cd ..
nano .env.local
# انسخ محتوى من .env.example وعدل القيم

# تابع من QUICK_START.md الخطوة 5
```

---

## الطريقة 3: استخدام WinSCP

### الخطوة 1: تحميل WinSCP
- اذهب إلى: https://winscp.net/eng/download.php
- حمل وثبت WinSCP

### الخطوة 2: الاتصال
1. افتح WinSCP
2. **File protocol:** SFTP
3. **Host name:** YOUR-VPS-IP
4. **Port:** 22
5. **User name:** root
6. **Password:** كلمة مرور VPS
7. اضغط **Login**

### الخطوة 3: رفع الملفات
- نفس خطوات FileZilla

---

## ⚠️ مشاكل الطريقة اليدوية

❌ **صعوبة التحديثات** - كل تعديل يحتاج رفع يدوي  
❌ **لا توجد نسخ احتياطية** - إذا حذفت شيء، ضاع  
❌ **صعوبة التعاون** - لا يمكن لأكثر من شخص العمل  

---

## ✅ الحل الأفضل: تثبيت Git

**نوصي بشدة بتثبيت Git!** راجع `GIT_SETUP_GUIDE.md`

الفوائد:
- ✅ تحديثات سهلة
- ✅ نسخ احتياطية تلقائية
- ✅ تتبع التغييرات
- ✅ سهولة التعاون
- ✅ النشر التلقائي

---

**تاريخ الإنشاء:** 2025-11-26
