# 📦 دليل تثبيت Git وإعداد المشروع

## الخطوة 1: تثبيت Git على Windows

### الطريقة 1: تحميل من الموقع الرسمي
1. اذهب إلى: https://git-scm.com/download/win
2. حمل النسخة المناسبة (64-bit أو 32-bit)
3. شغل الملف المحمل
4. اتبع خطوات التثبيت (اضغط Next على كل شيء)
5. بعد التثبيت، أعد فتح PowerShell

### الطريقة 2: باستخدام Winget (إذا كان متوفراً)
```powershell
winget install --id Git.Git -e --source winget
```

### الطريقة 3: باستخدام Chocolatey (إذا كان مثبتاً)
```powershell
choco install git
```

---

## الخطوة 2: التحقق من التثبيت

بعد تثبيت Git، أعد فتح PowerShell وجرب:
```powershell
git --version
```

يجب أن ترى شيئاً مثل: `git version 2.43.0`

---

## الخطوة 3: إعداد Git (مرة واحدة فقط)

```powershell
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

---

## الخطوة 4: إنشاء Repository على GitHub

1. اذهب إلى: https://github.com/new
2. اسم Repository: `portail-sila` (أو أي اسم تريده)
3. اجعله **Private** (لحماية البيانات الحساسة)
4. **لا تضف** README أو .gitignore أو License
5. اضغط **Create repository**

---

## الخطوة 5: رفع الكود على GitHub

بعد إنشاء Repository، شغل هذه الأوامر في PowerShell:

```powershell
# الانتقال إلى مجلد المشروع
cd d:\portail

# تهيئة Git
git init

# إضافة جميع الملفات
git add .

# أول Commit
git commit -m "Initial commit - Portail SILA"

# ربط مع GitHub (غير USERNAME و REPO-NAME)
git remote add origin https://github.com/USERNAME/REPO-NAME.git

# رفع الكود
git branch -M main
git push -u origin main
```

**ملاحظة:** عند الرفع، قد يطلب منك GitHub:
- اسم المستخدم (Username)
- Personal Access Token (بدلاً من كلمة المرور)

### كيفية الحصول على Personal Access Token:
1. اذهب إلى: https://github.com/settings/tokens
2. اضغط **Generate new token** → **Generate new token (classic)**
3. اسم Token: `portail-sila`
4. صلاحيات: اختر **repo** (كل الصلاحيات)
5. اضغط **Generate token**
6. **انسخ Token** (لن تراه مرة أخرى!)
7. استخدمه بدلاً من كلمة المرور عند الرفع

---

## الخطوة 6: التحقق من الرفع

اذهب إلى:
```
https://github.com/USERNAME/REPO-NAME
```

يجب أن ترى جميع ملفات المشروع! ✅

---

## 🚀 بعد رفع الكود على GitHub

### الخطوة التالية: النشر على VPS

1. **اتصل بـ VPS:**
   ```bash
   ssh root@YOUR-VPS-IP
   ```

2. **اتبع QUICK_START.md** من الخطوة 3 فصاعداً

---

## ⚠️ ملاحظات مهمة

### ملفات محمية (لن يتم رفعها):
- `.env` (Backend)
- `.env.local` (Frontend)
- `node_modules/`
- `dist/`

هذه الملفات محمية بواسطة `.gitignore` ✅

### ملفات سيتم رفعها:
- `.env.example` (نماذج فقط)
- جميع ملفات الكود
- مجلد `public/` (مع اللوقو)
- جميع ملفات التوثيق

---

## 🆘 مشاكل شائعة

### 1. Git لا يعمل بعد التثبيت
**الحل:** أعد فتح PowerShell أو أعد تشغيل الكمبيوتر

### 2. خطأ في git push
**الحل:** تأكد من:
- اسم المستخدم صحيح
- استخدمت Personal Access Token (وليس كلمة المرور)
- Repository موجود على GitHub

### 3. الملفات الحساسة ظهرت على GitHub
**الحل:** 
```powershell
git rm --cached .env
git rm --cached backend/.env
git commit -m "Remove sensitive files"
git push
```

---

**تاريخ الإنشاء:** 2025-11-26  
**الحالة:** جاهز للتنفيذ 🚀
