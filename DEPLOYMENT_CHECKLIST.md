# ✅ Checklist النشر الكامل

## 📋 قبل البدء

### معلومات VPS
- [ ] عنوان IP: `___________________`
- [ ] كلمة مرور Root: `___________________`
- [ ] الوصول عبر SSH يعمل

### معلومات قاعدة البيانات
- [ ] DB_HOST: `___________________`
- [ ] DB_USER: `u557748645_yami`
- [ ] DB_PASSWORD: `___________________`
- [ ] DB_NAME: `___________________`

### معلومات Domain
- [ ] Domain يشير إلى IP الخاص بـ VPS
- [ ] DNS محدث (A Record)

### API Keys
- [ ] GEMINI_API_KEY جاهز
- [ ] JWT_SECRET محدد (32+ حرف عشوائي)

---

## 🔧 إعداد VPS

- [ ] تم الاتصال بـ VPS عبر SSH
- [ ] تم تحديث النظام (`apt update && apt upgrade`)
- [ ] تم تثبيت Node.js 20.x
- [ ] تم تثبيت Git
- [ ] تم تثبيت PM2
- [ ] تم تثبيت Nginx
- [ ] تم تثبيت Certbot
- [ ] تم إعداد Firewall (UFW)

---

## 📦 رفع الكود

- [ ] تم إنشاء Repository على GitHub
- [ ] تم رفع الكود على GitHub
- [ ] تم استنساخ المشروع على VPS في `/var/www/portail-sila`
- [ ] الصلاحيات صحيحة (`chown -R $USER:$USER`)

---

## 🔧 Backend

- [ ] تم إنشاء ملف `.env` في `backend/`
- [ ] جميع متغيرات البيئة معبأة بشكل صحيح
- [ ] تم تشغيل `npm install`
- [ ] تم تشغيل `npm run build`
- [ ] لا توجد أخطاء في البناء
- [ ] تم تشغيل Backend مع PM2
- [ ] PM2 يظهر حالة `online`
- [ ] لا توجد أخطاء في `pm2 logs`

---

## 🎨 Frontend

- [ ] تم إنشاء ملف `.env.local`
- [ ] GEMINI_API_KEY معبأ
- [ ] VITE_API_BASE_URL يشير إلى `https://tchadcare.com/api`
- [ ] تم تشغيل `npm install`
- [ ] تم تشغيل `npm run build`
- [ ] لا توجد أخطاء في البناء
- [ ] مجلد `dist/` موجود ويحتوي على ملفات

---

## 🌐 Nginx

- [ ] تم نسخ `nginx.conf` إلى `/etc/nginx/sites-available/tchadcare.com`
- [ ] تم إنشاء symbolic link في `sites-enabled`
- [ ] تم حذف الموقع الافتراضي
- [ ] `nginx -t` يظهر نجاح الاختبار
- [ ] تم إعادة تشغيل Nginx
- [ ] Nginx يعمل (`systemctl status nginx`)

---

## 🗄️ قاعدة البيانات

- [ ] تم الحصول على IP الخاص بـ VPS (`curl ifconfig.me`)
- [ ] تم إضافة IP في Remote MySQL على Hostinger
- [ ] تم اختبار الاتصال بقاعدة البيانات
- [ ] الجداول تم إنشاؤها تلقائياً
- [ ] المسؤول الافتراضي موجود (DPASSAHS)

---

## 🔒 SSL/HTTPS

- [ ] تم تشغيل Certbot
- [ ] تم الحصول على شهادة SSL بنجاح
- [ ] Nginx محدث تلقائياً للـ HTTPS
- [ ] إعادة التوجيه من HTTP إلى HTTPS يعمل
- [ ] الموقع يفتح على `https://tchadcare.com`
- [ ] لا توجد تحذيرات SSL في المتصفح

---

## 🔥 Firewall

- [ ] UFW مفعل
- [ ] OpenSSH مسموح
- [ ] Nginx Full مسموح
- [ ] `ufw status` يظهر القواعد الصحيحة

---

## ✅ اختبارات نهائية

### الموقع
- [ ] `https://tchadcare.com` يفتح بنجاح
- [ ] الصفحة الرئيسية تظهر بشكل صحيح
- [ ] لا توجد أخطاء في Console المتصفح

### تسجيل الدخول - Admin
- [ ] صفحة تسجيل دخول المسؤول تفتح
- [ ] تسجيل الدخول بـ `DPASSAHS` / `DPASSAHS@2025` يعمل
- [ ] لوحة تحكم المسؤول تظهر

### API
- [ ] `https://tchadcare.com/api/health` يرجع استجابة
- [ ] لا توجد أخطاء CORS
- [ ] Backend يستجيب للطلبات

### قاعدة البيانات
- [ ] يمكن جلب المنظمات
- [ ] يمكن جلب المشاريع
- [ ] يمكن إضافة بيانات جديدة

---

## 📊 المراقبة

- [ ] `pm2 status` يظهر Backend يعمل
- [ ] `pm2 logs` لا يظهر أخطاء
- [ ] `systemctl status nginx` يظهر active
- [ ] السجلات في `/var/log/nginx/` طبيعية

---

## 🔄 التحديثات المستقبلية

- [ ] سكريبت `deploy.sh` قابل للتنفيذ (`chmod +x`)
- [ ] تم اختبار سكريبت التحديث
- [ ] PM2 معد للتشغيل التلقائي عند إعادة التشغيل

---

## 📝 التوثيق

- [ ] README.md محدث
- [ ] DEPLOYMENT_GUIDE.md موجود
- [ ] QUICK_START.md موجود
- [ ] PROJECT_STRUCTURE.md موجود
- [ ] ملفات `.env.example` موجودة

---

## 🎯 النتيجة النهائية

عند إكمال جميع النقاط أعلاه:

✅ الموقع يعمل على: **https://tchadcare.com**  
✅ Backend API يعمل على: **https://tchadcare.com/api**  
✅ قاعدة البيانات متصلة ومحدثة  
✅ SSL/HTTPS مفعل  
✅ PM2 يدير Backend  
✅ Nginx يعمل كـ Reverse Proxy  
✅ جميع الميزات تعمل بشكل صحيح  

---

## 🆘 في حالة وجود مشاكل

### Backend لا يعمل
```bash
pm2 logs portail-backend
pm2 restart portail-backend
```

### قاعدة البيانات
```bash
cat /var/www/portail-sila/backend/.env | grep DB_
# تحقق من Remote MySQL في Hostinger
```

### Nginx
```bash
sudo nginx -t
sudo systemctl restart nginx
sudo tail -f /var/log/nginx/error.log
```

### SSL
```bash
sudo certbot renew --dry-run
sudo certbot certificates
```

---

**📅 تاريخ النشر:** ___________________  
**👤 تم بواسطة:** ___________________  
**✅ الحالة:** ___________________  

---

## 🎉 تهانينا!

إذا أكملت جميع النقاط أعلاه، فقد نجحت في نشر **Portail HUMANITAIRES** بالكامل! 🚀

**الموقع الآن متاح على:** https://tchadcare.com
